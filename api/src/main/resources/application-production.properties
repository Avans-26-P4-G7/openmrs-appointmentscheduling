# Productie omgeving configuratie
environment=production

# Database
db.url=${DB_URL}
db.username=${DB_USERNAME}
db.password=${DB_PASSWORD}

# Logging (minimaal in productie)
log.level=WARN

# Testdata is nooit toegestaan in productie
allow.test.data=false
